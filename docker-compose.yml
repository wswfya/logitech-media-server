version: '3.8'
services:
  lms:
    build: .  # 自动读取当前目录的Dockerfile（已替换为squeezebox/lms基础镜像）
    container_name: lms-server
    restart: always
    network_mode: host
    volumes:
      - ./config:/srv/squeezebox  # 持久化LMS配置
      - ./music:/music             # 挂载音乐目录
